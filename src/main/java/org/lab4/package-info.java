/**
 * Contains classes for managing and loading data related to personnel information.
 *
 * <p>This package includes classes such as `Person` and `Division`, which represent individuals and organizational divisions, respectively. Additionally, it contains utilities like `CSVLoader` and `CSVLoaderPerson` for loading personnel data from CSV files into corresponding Java objects.</p>
 */
package org.lab4;